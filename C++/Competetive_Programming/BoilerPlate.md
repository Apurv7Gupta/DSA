```cpp
#include <bits/stdc++.h>
using namespace std;

void solve()
{
    // ================inputs==============
    // ====================================

    // your solution here
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;

    while (t--)
    {
        solve();
    }

    return 0;
}
```

In Codeforces:

- you do "cout" instead of "return"
- always end cout with "\n"
- always include the sync_with_stdio to inc speed

- you have to take in the test cases and run a while loop until all testcases are over
- the solution code has been separated in the solve() function
